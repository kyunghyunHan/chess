extern crate args;
extern crate getopts;

use chess::{All_RANKS, Board, BoardStatus, ChessMove, Color, MobveGen, Piece};
//체스?/
use std::env;
use std::io::{self, BufRead};

const STARTING_FEN: &str;
fn main() {
    println!("체스둘거임");
}
